# antiX default minimal touchpad config (pre-tuning baseline).
# Use this to revert to stock antiX behaviour.
Section "InputClass"
Identifier "touchpad"
Driver "libinput"
  MatchIsTouchpad "on"
  Option "Tapping" "on"
EndSection
